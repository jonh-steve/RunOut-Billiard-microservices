// 🩷 Vị trí: api-gateway/src/middleware/auth.middleware.js
// Middleware xác thực cho API Gateway (anh yêu dễ thương tự chỉnh sửa nhé)
module.exports = (req, res, next) => {
  // TODO: Xác thực request
  next();
};
